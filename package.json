{
  "name": "virtual-photo-room",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"cd server && bun run dev\" \"cd client && bun run dev --host 0.0.0.0\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "simple-peer": "^9.11.1"
  },
  "devDependencies": {
    "@types/simple-peer": "^9.11.9",
    "events": "^3.3.0",
    "process": "^0.11.10",
    "util": "^0.12.5"
  }
}
